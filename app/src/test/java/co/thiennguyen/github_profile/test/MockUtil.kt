package co.thiennguyen.github_profile.test

import co.thiennguyen.github_profile.domain.models.Model

object MockUtil {

    val models = listOf(
        Model(id = 1),
        Model(id = 2),
        Model(id = 3),
    )
}
